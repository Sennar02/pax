#ifndef PAX_MEMORY_DEFINE_HPP
#define PAX_MEMORY_DEFINE_HPP

#include <pax/Base/import.hpp>

namespace pax
{
    struct Alloc;
    struct Bump_Alloc;
    struct Pool_Alloc;

    struct Builder;

    template <class Type>
    struct Array;

    template <class Type>
    struct Array2d;
} // pax

#endif // PAX_MEMORY_DEFINE_HPP
