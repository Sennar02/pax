// #include "demo/Maze.hpp"

// #include <time.h>
// #include <unistd.h>

void
test_engine()
{
    // Maze         maze;
    // Maze_Builder builder = maze_builder_create(maze);

    // maze.acquire({21u, 9u});

    // builder.prepare({
    //     maze.array.size(0) / 2u,
    //     maze.array.size(1) / 2u,
    // });

    // srand(clock());

    // builder.build();

    // maze.write();
}
